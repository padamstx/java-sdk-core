# This workflow is responsible for:
# - publishing artifacts to Maven Central
# - building and publishing javadocs to the git repository.
# It is triggered when a new release is created.

name: Publish Release
on:
  release:
    types: [created]
  workflow_dispatch:
    # Allow this workflow to be triggered manually

jobs:
  publish:
    name: Publish Release
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Java 
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'adopt'
        cache: 'maven'
        # Configure ~/.m2/settings.xml
        server-id: ossrh
        server-username: MAVEN_USERNAME
        server-password: MAVEN_PASSWORD
        # import GPG key into build agent's local keystore
        gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
        gpg-passphrase: ${{ secrets.GPG_PASSPHRASE }}

    - name: Set artifact version
      run: |
        mvn versions:set -DnewVersion=${GITHUB_REF_NAME} -DgenerateBackupPoms=false
        echo "Set artifact version to: ${GITHUB_REF_NAME}"

    - name: Display some stuff for debugging
      run: |
        echo ">>> settings.xml:"
        cat ~/.m2/settings.xml
        echo ">>> pom.xml:"
        cat pom.xml

    - name: Publish javadocs to Github Pages
      run: |
        mvn clean javadoc:javadoc
        build/publishJavadoc-ghactions.sh
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}

    - name: Publish artifacts to Maven Central
      env:
        MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
        MAVEN_PASSWORD: ${{ secrets.OSSRH_PASSWORD }}
        GPG_KEYNAME: ${{ secrets.GPG_KEYNAME }}
        GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      run: |
        echo "Publishing to maven central using username: ${MAVEN_USERNAME}"
        # build only the "sign" goal for now until we're brave enough to try to deploy to MC :)
        mvn -B sign
